import { useState } from "react";
import "./App.css";
import AllRoutes from "./Components/AllRoutes";

function App() {
  return (
    <div className="App">
      <AllRoutes />
    </div>
  );
}

export default App;


// This is an ecommerce site created via React Js for frontend.
// And I used Json-server for backend so pls first run (npm run json-server).
// to see the project. otherwise it shows nothing. Thank You.